[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NEXT_PUBLIC_APP_URL = "https://blockwin.space

# Next.js plugin for Netlify - handles SSR and API routes
[[plugins]]
  package = "@netlify/plugin-nextjs"
